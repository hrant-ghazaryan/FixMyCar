@model FixMyCar.Web.Models.User

@{
    ViewData["Title"] = "Profile";
    var posts = ViewBag.UserPosts as List<FixMyCar.Web.Models.Post>;
}

<h2>My Profile</h2>

<div class="card p-3 mb-4">

    <h4>@Model.UserName</h4>

    <p><b>Email:</b> @Model.Email</p>
    <p><b>Phone:</b> @Model.PhoneNumber</p>
    <p><b>Created:</b> @Model.CreatedAt</p>

</div>

<hr />

<h3>My Posts</h3>

@if (posts != null && posts.Any())
{
    <table class="table table-striped">
        <thead>
            <tr>
                <th>Title</th>
                <th>City</th>
                <th>Category</th>
                <th></th>
            </tr>
        </thead>

        <tbody>
            @foreach (var item in posts)
            {
                <tr>
                    <td>@item.Title</td>
                    <td>@item.City</td>
                    <td>@item.Category?.Name</td>
                    <td>
                        <a asp-controller="Post"
                               asp-action="Details"
                               asp-route-id="@item.Id"
                               class="btn btn-sm btn-info">
                            View
                        </a>

                        <a asp-controller="Post"
                               asp-action="Edit"
                               asp-route-id="@item.Id"
                               class="btn btn-sm btn-warning">
                            Edit
                        </a>

                        <a asp-controller="Post"
                               asp-action="Delete"
                               asp-route-id="@item.Id"
                               class="btn btn-sm btn-danger">
                            Delete
                        </a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}
else
{
    <p>No posts yet.</p>
}
<hr />

<h3>My Offers</h3>

@{
    var offers = ViewBag.UserOffers as List<FixMyCar.Web.Models.Offer>;
}

@if (offers != null && offers.Any())
{
    <table class="table table-striped">

        <thead>
            <tr>
                <th>Price</th>
                <th>Message</th>
                <th>Post</th>
                <th>Created</th>
                <th></th>
            </tr>
        </thead>

        <tbody>
            @foreach (var offer in offers)
            {
                <tr>
                    <td>@offer.Price $</td>
                    <td>@offer.Message</td>
                    <td>@offer.Post?.Title</td>
                    <td>@offer.CreatedAt</td>

                    <td class="text-end">

                        <!-- View -->
                        <a asp-controller="Offer"
                               asp-action="Details"
                               asp-route-id="@offer.Id"
                               class="btn btn-sm btn-info">
                            View
                        </a>

                        <!-- Edit -->
                        <a asp-controller="Offer"
                               asp-action="Edit"
                               asp-route-id="@offer.Id"
                               class="btn btn-sm btn-warning">
                            Edit
                        </a>

                        <!-- Delete -->
                        <a asp-controller="Offer"
                               asp-action="Delete"
                               asp-route-id="@offer.Id"
                               class="btn btn-sm btn-danger">
                            Delete
                        </a>

                    </td>
                </tr>
            }
        </tbody>

    </table>
}
else
{
    <p>No offers yet.</p>
}